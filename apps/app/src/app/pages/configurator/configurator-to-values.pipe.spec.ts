import { ConfiguratorToValuesPipe } from './configurator-to-values.pipe';

describe('ConfiguratorToValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new ConfiguratorToValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
